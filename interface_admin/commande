sudo setfacl -m u:www-data:rwx /home/station-blanche/quarantaine
sudo setfacl -d -m u:www-data:rwx /home/station-blanche/quarantaine

sudo apt install apache2 openssl

sudo a2enmod ssl
sudo a2enmod headers
sudo a2enmod rewrite
sudo mkdir -p /etc/ssl/private
sudo openssl req -x509 -nodes -days 365   -newkey rsa:2048   -keyout /etc/ssl/private/apache-selfsigned.key   -out /etc/ssl/certs/apache-selfsigned.crt   -subj "/C=FR/ST=France/L=Paris/O=MonSiteLocal/OU=IT/CN=station-blanche.local"
sudo a2ensite 000-default.conf
sudo a2ensite 000-default-ssl.conf
sudo systemctl restart apache2

CREATE USER 'admin'@'%' IDENTIFIED BY '252106';
GRANT ALL PRIVILEGES ON `station_blanche`.* TO 'admin'@'%' WITH GRANT OPTION;
FLUSH PRIVILEGES;